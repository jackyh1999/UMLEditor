package model.line;

import java.awt.Graphics;
import java.awt.Polygon;

public class AssociationLine extends LineType{
	
	/*
	int x1,x2,x3,y1,y2,y3;
	public AssociationLine(MyLine line) {
		this.begin_port = line.begin_port;
		this.end_port = line.end_port;
		this.vec = line.vec;
		SetCoordinate();
	}
	
	@Override
	public void DrawLine(Graphics g) {
		g.drawLine(x1,y1,x2,y2);
		g.drawLine(x1,y1,x3,y3);
	}
	
	public void SetCoordinate() {
		this.x1 = this.end_port.GetCenterX();
		this.y1 = this.end_port.GetCenterY();
		this.x2 = this.end_port.GetCenterX() + (int)(vec[1]*(-10)) + (int)(vec[0]*(-10));
		this.y2 = this.end_port.GetCenterY() + (int)(vec[0]*10) + (int)(vec[1]*(-10));
		this.x3 = this.end_port.GetCenterX() + (int)(vec[1]*10) + (int)(vec[0]*(-10));
		this.y3 = this.end_port.GetCenterY() + (int)(vec[0]*(-10)) + (int)(vec[1]*(-10));
	}
	*/
	
}
